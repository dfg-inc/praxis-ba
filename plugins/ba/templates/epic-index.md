---
id: E1
type: epic
title: Template Epic
status: active
---

<!-- TEMPLATE (Task 17): `id` above is a FORMAT-VALID SENTINEL (E1) —
replace it with the id returned by `praxis-ba id next --scope epic`
(minted+persisted; the hybrid direct-Write contract, not an `epic add`
call); this file is
that epic's `epics/E{n}-slug/index.md`. `title` is free text — replace
"Template Epic" with the real epic name. `status` here is free text too
(no fixed enum on the epic itself, unlike FR/NFR/BR's unified lifecycle) —
a short human-legible word like "active"/"done"/"paused" is enough; this
page's FR/NFR rollups (which of them exist, and their own statuses) are
always DERIVED from the graph, never hand-maintained in this frontmatter. -->

Describe the epic: the slice of the vision it covers, and roughly what
capability exists once every FR under it is delivered. Keep it short — the
authoritative detail lives in the FRs/NFRs themselves, not duplicated here.
