---
type: vision
status: draft
---

<!-- TEMPLATE (Task 17): replace this whole body with the confirmed vision
statement. Leave `status: draft` in the frontmatter until a human runs
`vision confirm` — that verb is the ONLY writer allowed to set `status:
confirmed` (+ `confirmed_at`/`confirmed_by`), per the design's human-gate
posture (§7). Definition-of-ready and the accept gate both read
`status == confirmed`, so no WP can reach `ready` while this is `draft`. -->

State, in one or two paragraphs, what this product is for and the outcome
it exists to produce — the single sentence a stranger could use to judge
whether any future FR belongs in scope. Write it in plain prose; there is
no machine-checked grammar for this body, only the frontmatter status gate.
