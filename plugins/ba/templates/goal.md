---
id: G1
type: goal
title: <one-line outcome the product exists to produce>
status: draft
---

<!-- TEMPLATE (WBS 1.11): `id` is a FORMAT-VALID SENTINEL (G1) — replace with
the next free G{n} under goals/ (scan existing goals/*.md; there is no
`id next --scope goal` yet). Linked requirements declare this id in their
`goal_ids` frontmatter; the requirement list on this page is a derived
backlink (`praxis-ba goals status` / graph.goalRequirements), never stored here. -->

## Outcome

State the measurable product outcome this goal represents — what becomes
true for the user or the business when the linked requirements ship.

## Success signal

How a human will know this goal is met (a metric, a behaviour, a decision
gate) — not a list of features.
